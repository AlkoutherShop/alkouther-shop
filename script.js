 document.getElementById('payment-method').addEventListener('change', function() {
       
        // إظهار الخيارات الإضافية عند الاختيار
        if (this.value === 'cash' || this.value === 'card') {
            // يمكنك إضافة أي تعديلات هنا
        }
    });
